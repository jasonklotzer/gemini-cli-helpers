{
  "name": "gemini-cli-helpers",
  "version": "1.0.0",
  "description": "A collection of helper scripts for the Gemini CLI.",
  "bin": {
    "gemit": "scripts/gemit.sh"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version:patch": "npm version patch -m 'Bump version to %s'",
    "version:minor": "npm version minor -m 'Bump version to %s'",
    "version:major": "npm version major -m 'Bump version to %s'",
    "release": "npm version patch -m 'Bump version to %s' && git push && git push --tags"
  },
  "keywords": [
    "gemini",
    "cli",
    "git",
    "commit"
  ],
  "author": "Jason Klotzer",
  "license": "Apache-2.0"
}
